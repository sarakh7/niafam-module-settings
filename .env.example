# Vite Environment Variables
# Copy this file to .env for local development or .env.production for production builds

# Base path for deployment
# Set this to match your server deployment path
# Examples:
#   VITE_BASE_PATH=/                        (for root deployment)
#   VITE_BASE_PATH=/uploads/starling/       (for subdirectory deployment)
VITE_BASE_PATH=/
